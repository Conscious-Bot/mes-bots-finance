# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
.venv/
ENV/
*.egg-info/

# Secrets — NEVER commit
.env
.env.*
!.env.example
*.key
*.pem
oauth_tokens/

# DB (tracked via separate snapshots in ~/backups)
data/bot.db
data/bot.db-journal
data/bot.db-wal
data/bot.db-shm
*.db
*.sqlite
*.sqlite3

# Local backups (already separately backed up to ~/backups)
.backups/

# Logs
*.log
bot.log
nohup.out

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo

# Models / embeddings cache
.cache/
models/

# Audit reports (timestamped, don't track all)
AUDIT_*.md
!AUDIT_2026-05-12_marathon.md

# === Secrets additionnels (OAuth tokens etc.) ===
token.json
*.token
*.tokens
credentials.json
client_secret*.json
.last_alert
service-account*.json

# === Backup pollution (cleanup .backup_avant_*, TODO.bak, etc.) ===
*.backup_avant_*
*.bak
*.bak.*
*.preTriage_*
TODO.bak.*
TODO.preTriage_*

# === Weird accidental files ===
40%

# === Pre-existing artifacts cleanup ===
.coverage
data/backups/
data/cache/
data/bot_state.json
*.bak.preTierA

# DB snapshots (Sprint 1.3 onwards)
data/bot.db.pre_*
data/bot.db.bak*

# DB snapshots (Sprint 1.3 onwards)
data/bot.db.pre_*
data/bot.db.bak*

# Local defensive snapshots (cp file file.bak_TIMESTAMP)
*.bak_*
*.before_restart_*
uptime.log.pre_case_fix_*
bot.log*
*.keep

# Bot log rotations + temp files (Day 12 cleanup)
bot.log.*
token.json.dead_*

# generated dashboard output
dashboard/*.html

# secrets (NE JAMAIS committer)
.env
.env.*
token.json
token.json.*
credentials.json

# data / state
data/
*.db
*.db-shm
*.db-wal

# python
__pycache__/
*.pyc
.coverage
htmlcov/

# backups / logs
*.bak
*.bak.*
*.bak_*
*.backup_avant_*
*.log
*.log.*
.backups/

# Reconcile broker exports (personal financial data)
scripts/reconcile_exports/*.csv

# Skill installs (perso, reinstaller via clone)
.claude/skills/ui-ux-pro-max/
credentials.json.pre-*

# Artefacts opérationnels (cron + backups + digests) — 13/06/2026
backups/
digests/
.j_day_watcher_alert_fired

# Chroma vector DB local (Voyage thesis library)
data/thesis_chroma/

# Cron-generated skeleton calibration audits (audit_calibration_job, daily).
# Substantive findings local-only ; pro audits (v4/v5/etc) stay tracked.
docs/calibration_audits/*_skeleton.md

# Package trial build output (regénérable via packaging/trial/build_package.sh)
dist/
# Cruft hygiene (leçon packaging 30/06)
*.bak_*
bot.log.*
!bot.log.*.keep
logs/
config.yaml.backup*
