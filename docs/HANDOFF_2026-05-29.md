# HANDOFF Session 2026-05-29 — Sprint 5 à 19 + user_strategy

**Date** : 29 mai 2026, Day 24
**Auteur** : Olivier Legendre + Claude
**Durée session** : journée complète (53 commits)
**État final** : tout pushé sur main, B- 65 → A+ 91/100 après recalibration honnête

---

## TL;DR

On a transformé PRESAGE d'un dashboard polish en un **système adversarial co-pilot avec boucle vivante fermée** :
- Récolte (chat persisté, signaux extraits) → Analyse (intent extraction, soft signals) → Digestion (user_profile, Layer 2 conceptions, Layer 3 preferences) → Précision (gates qui mordent, simulation grade, copilot pre-trade) → Action (trades chat-driven multi-action)
- Source de vérité unique = `ticker_axes` (driver/stage/moat/macro_factor) — propage en cascade
- Stratégie utilisateur déclarée explicitement (concentrator_thematic, target 75%, benchmark SOXX)

## Architecture finale

### Pages dashboard (7)
1. **Vue d'ensemble** : hero Valeur+Cockpit + Note PF + Chat copilot + Kill-criteria + Pressions copilot + Cockpit discipline + Plus proches cible / Marges faibles + Hausses/baisses jour + Échéances + Journal
2. **Positions** : par broker + détail
3. **Thèses** : 28 actives
4. **Stratégie** (NEW) : ta stratégie déclarée + Lecture du livre (trajectory/factor/stress/FX/alpha) + Concentration cachée (SPOF/Mauboussin/valo>bull) + Doublons mesurés (correlation+axes) + Apprentissage du bot (conceptions/preferences/soft signals/conversations) + Placement fiscal
5. **Concentration** : clusters par secteur/pays
6. **Signaux** : flux newsletter
7. **Urgence** : par downside

### Pages SVG icons
- Vue d'ensemble : radar arc
- Positions : layered cube
- Thèses : nested circles
- Stratégie : couronne (crown - chess king)
- Concentration : circle with arms
- Signaux : sound waves
- Urgence : triangle alert

### Tables DB nouvelles (11)
- `chat_messages` : persistance brute chat (user+assistant, surface, session_id)
- `chat_extracted_signals` : soft signals captés passivement par message
- `bot_conceptions` (Layer 2) : vue stable du bot per ticker
- `bot_preferences` (Layer 3) : ce qui marche déterministically pour CE user (6 kinds)
- `ticker_axes` : tagging strict (demand_driver / value_chain_stage / moat_source / macro_factor)
- `ticker_meta` : Mauboussin (fade_rate / upstream_critical_deps / valo)
- `kill_criteria_alerts` : monitor invalidation_triggers
- `data_clusters_snapshots` : correlation rendements clusters (Sprint 17)
- `portfolio_grades` : snapshot quotidien
- `portfolio_narrative_clusters` : (deprecated Sprint 18)
- `user_profile` : synthèse archetype / dialogue_tone / bias_signature

### Crons clé (35 total)
- `daily_portfolio_grade_job` 23h15 — snapshot quotidien grade
- `daily_kill_criteria_check_job` 07h30 — verification dormant/at_risk/triggered + push Telegram sur transition
- `weekly_user_profile_refresh_job` Dim 21h00 — ingere chat_history + signals
- `weekly_bot_conceptions_synthesis_job` Dim 19h00 — vue stable per ticker
- `weekly_portfolio_narrative_synthesis_job` Dim 20h30 — narrative LLM (deprecated)
- `weekly_data_clusters_synthesis_job` Sat 18h00 — clusters par correlation
- `monthly_bot_preferences_synthesis_job` 1er 04h00 — calibration empirique
- `resolve_copilot_interventions_30d_job` 09h15 — J+30 outcome resolve

### Modules intelligence nouveaux
- `portfolio_grade.py` : 7 dims (quality_T1_plus, T2_redondant, decorrelation_star, sizing_conviction, cluster_cap, thesis_health, **cycle_valo_exposure**) + gates plafond/plancher + user_strategy honored
- `portfolio_grade_llm.py` : Sprint 6 narrative (DEPRECATED Sprint 19)
- `decision_copilot.py` : pre-trade adversarial avec blocks user_profile / bot_conception / bot_preferences / grade_simulation
- `bot_conceptions.py` : Layer 2 synthèse
- `bot_preferences.py` : Layer 3 extractors
- `chat_intent.py` : extract_intent (multi-intent compound) + execute_intents (list) + EUR/full_exit support + 11 kinds
- `factor_exposures.py` : decomposition + stress tests 7 scenarios
- `spof_and_sizing.py` : SPOF graph + Mauboussin implied + valo>bull
- `wrapper_tax.py` : PEA/CTO + tax-loss + FX exposure
- `benchmark.py` : alpha vs SOXX (user-configurable)
- `return_clustering.py` : correlation matrix + hierarchical clustering
- `ticker_classifier.py` + `ticker_meta_classifier.py` : Sonnet enrich, ~$0.025/ticker
- `kill_criteria_monitor.py` : Haiku evaluate + push Telegram transition

### Boucle complète (RÉCOLTE → ANALYSE → DIGESTION → PRÉCISION)

```
RECOLTE                       ANALYSE                       DIGESTION
─────────                     ─────────                     ─────────
chat_messages              chat_extracted_signals        user_profile (Sprint 2)
decisions                  ticker_axes                   bot_conceptions L2
theses                     ticker_meta                   bot_preferences L3
positions                  portfolio_narrative_clusters  
copilot_intervent.         (deprecated)
signals                                                  
predictions resolved       ↓                              ↓
                                                          
                       PRECISION (action)                ALERTES/MONITOR
                       ─────────                         ─────────
                       Note PF 7 dims (gates)            kill_criteria_alerts
                       Factor exposures                  Telegram notify
                       Stress tests                      Daily 07h30
                       SPOF graph                        
                       Mauboussin sizing                 
                       Valo > bull case                  
                       Copilot pre-trade (blocks RAG)    
                       Chat tailormade                   
                       simulate_grade (avant/apres)      
                       Trade execution (compound chat)   
```

## TODOs ouverts (next session)

### Time-bound (irreductibles, attendre ~30j)
1. **Layer 3 preferences** : actuellement n=0 partout. Calibration empirique commence quand decisions atteignent J+30 (premier batch 10-15 juin)
2. **Copilot backtest** : `resolve_copilot_interventions_30d_job` actif mais sample 0 — meme echeance que #1

### High-impact (à attaquer)
3. **PEA/CTO wrapper enforcement** : SU.PA en CTO mais theoriquement PEA-eligible — verifier si user veut migrer
4. **17 kill-criteria at_risk** : pre-alert push activé mais l'user peut consolider via chat ce qu'il veut surveiller activement
5. **Multi-turn Telegram /chat** : single-shot pour l'instant, session_id `tg_<uid>` deja en place — facile à activer
6. **Drift Layer 2** : conceptions per-ticker week-over-week — quand snapshots s'accumulent dimanche après dimanche

### Plus loin (post-10/06)
7. **Migration site public PRESAGE** : React + Motion vanilla (motion.dev) sans build
8. **Auto-rebalancing proposals** : simulate_grade existe, manque l'optimizer qui propose des trades pour atteindre target
9. **Universe trim** : config.yaml.universe trop large (250+ tickers), trim au canonical + adjacents pertinents

## État des sauvegardes

- DB backup : `/Users/olivierlegendre/mes-bots-finance/backups/bot_20260529_1549_endsession.db` (8.8 MB)
- Schema doc regen : `docs/REFERENCE_SCHEMA.md` (47 tables, 71 indexes)
- Tests : **352 passed**, 0 fail
- Lint : ruff 0 erreur
- Git : tout pushé sur `main` (commits depuis le 23 mai inclus session 29/05)

## État live

- `serve.py` redémarré aujourd'hui (PID 71180), reload modules deps automatique
- Dashboard servé : http://127.0.0.1:8000/dashboard.html
- Polling auto-reload : 10 min (avec garde-fou isTyping si user dans chat)
- Meta refresh : 30 min
- Chat localStorage : derniers 40 messages + draft textarea persisté

## Commande de restart si besoin

```
! kill -9 $(pgrep -f "dashboard.serve") 2>/dev/null
! cd /Users/olivierlegendre/mes-bots-finance && nohup venv/bin/python -m dashboard.serve > dashboard/serve.log 2>&1 &
```

## Memoires Claude sauvegardees (session)

- `glossaire_canonique.md` — 5 axes + 2 notes (Construction + Fragilite)
- `feedback_plug_and_animate.md` — trigger manuel + valider live apres wiring
- `tax_loss_harvest_pending.md` — feature livrée mais saved for later
- `next_session_agenda.md` — chantiers prévus
- `presage_brand.md` — brand identity post-10/06
- `needle_canonical.md` — marker noir/blanc partout
- `viz_horizontal_bars.md` — horizontal bars pattern canonique

---

**Verdict honnête de la session** :

On a posé les fondations d'un vrai système (architecture qui tient, source unique, boucle vivante, gates qui mordent, stratégie utilisateur explicite). Les 53 commits portent une logique cohérente.

Items time-bound (Layer 3, copilot backtest) attendent la maturation des decisions au J+30. Pas une faute d'architecture.

Le système n'est pas un dashboard polish. C'est un système qui apprend de toi et te challenge.
