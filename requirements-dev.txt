# Dev/test dependencies (for CI + local dev)
pytest>=9.0.0
hypothesis>=6.150.0
pytest-cov>=7.0.0
ruff>=0.13.0
mypy>=2.1.0
types-requests>=2.33.0

# Pre-commit framework (hook ruff lint local pré-push, cf .pre-commit-config.yaml)
pre-commit>=4.0.0
fredapi>=0.5.2
